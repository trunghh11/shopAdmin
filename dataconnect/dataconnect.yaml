specVersion: "v1"
serviceId: "shopadmin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "shopadmin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
